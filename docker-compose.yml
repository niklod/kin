services:
  relay:
    build: .
    restart: unless-stopped
    ports:
      - "7778:7778"
    volumes:
      - relay-data:/data
    # Override listen address or config dir if needed:
    # command: ["--listen", "0.0.0.0:9000", "--config-dir", "/data"]

volumes:
  relay-data:
